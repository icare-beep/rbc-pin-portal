{
  "name": "rbc-pin-portal",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
```
Click **Commit changes**.

---

## 📁 Your repo should now have 4 files:
```
rbc-pin-portal/
├── bank_pin_registration.html
├── package.json
├── railway.json                ← new file
└── server.js
